#include <stdio.h>
/**
 * main - Entry point with two argument
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 *
 */
int main(int  __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
